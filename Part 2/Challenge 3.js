// 'expression4' evaluated to 'true' and '!' will reverse the output at 'expression5' which will be 'false'
// so the single line code will be like:
const result = !(((100 % 50) < (100 / 50)) && 300 + 5 === 305);

console.log(result);
// Output will be'false'